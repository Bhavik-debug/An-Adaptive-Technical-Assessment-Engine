# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Node / frontend (Phase 6)
node_modules/
dist/
.vite/

# Local data
*.sqlite3
uploads/
.DS_Store

# Embedding model weights (Day 8). Downloaded once from HuggingFace, ~67 MB.
# A model binary is not source; `pip install -e "./backend[embeddings]"` and the
# first run fetch it again on a clean clone.
.model-cache/
